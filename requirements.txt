# Core
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Anthropic (Claude) — Script Writer + Storyboard agents
anthropic>=0.28.0

# ElevenLabs TTS — Voiceover agent
elevenlabs>=1.3.0

# OpenAI (DALL-E 3) — Image generation
openai>=1.35.0

# Stability AI — alternative image backend (optional)
# Uncomment if using IMAGE_BACKEND=stability
# diffusers>=0.29.0
# torch>=2.3.0
# transformers>=4.42.0

# Runway — HTTP only, no official SDK
requests>=2.32.0

# Video assembly
moviepy>=2.0.0
# FFmpeg must be installed separately:
#   Ubuntu/Debian: sudo apt install ffmpeg
#   macOS:         brew install ffmpeg
#   Windows:       https://ffmpeg.org/download.html

# Audio duration probing
mutagen>=1.47.0

# Utilities
Pillow>=10.4.0
tqdm>=4.66.0

# Testing
pytest>=8.2.0
pytest-mock>=3.14.0
